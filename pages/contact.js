import React from 'react';
import Layout from '../components/layouts/Layout';

const contact = () => {
    return (
        <Layout>
            <p>contact</p>
        </Layout>
    );
};

export default contact;