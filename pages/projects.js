import React from 'react';
import Layout from '../components/layouts/Layout';

const projects = () => {
    return (
        <Layout>
            <p>projecs pelao</p>
        </Layout>
    );
};

export default projects;